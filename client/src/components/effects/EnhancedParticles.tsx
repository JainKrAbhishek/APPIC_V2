import React from 'react';

// Simplified placeholder component to replace the animation effects
const EnhancedParticles = (props: any) => {
  return (
    <div className="absolute inset-0 opacity-20 bg-gradient-to-tr from-primary/10 via-transparent to-primary/10"></div>
  );
};

export default EnhancedParticles;