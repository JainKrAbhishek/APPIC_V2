import React from 'react';

// Simplified placeholder component to replace the animation effects
export const TopEnhancedLight = (props: any) => {
  return (
    <div className="h-[300px] w-full bg-gradient-to-b from-primary/20 to-transparent"></div>
  );
};

export default TopEnhancedLight;