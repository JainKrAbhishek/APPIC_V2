/**
 * Compatibility layer for backward compatibility with existing imports
 * Re-exports components from the features/quantitative module
 */

import { 
  ContentViewer, 
  CategorySelector, 
  CategoryCard, 
  KnowledgeMap, 
  TopicList 
} from '@/features/quantitative';

export {
  ContentViewer,
  CategorySelector,
  CategoryCard,
  KnowledgeMap,
  TopicList
};