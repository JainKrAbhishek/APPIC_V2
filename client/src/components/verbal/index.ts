/**
 * Compatibility layer for backward compatibility with existing imports
 * Re-exports components from the features/verbal module
 */

import { ContentViewer, TypeSelector, TopicList } from '@/features/verbal';

export {
  ContentViewer,
  TypeSelector,
  TopicList
};